// Base configuration for talking to the PHP backend.
// Endpoints are not implemented yet — this is only the connection point
// that later phases (auth, game, leaderboard, etc.) will build on.

export const API_BASE_URL = "http://localhost/backend";
