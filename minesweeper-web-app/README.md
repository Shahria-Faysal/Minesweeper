# Minesweeper Web Application — Phase 1

Project skeleton: React (Vite) frontend + Plain PHP backend + MySQL.

## Structure
```
minesweeper-web-app/
├── frontend/        React + Vite app
├── backend/          PHP API (goes inside XAMPP htdocs)
├── database/
│   └── schema.sql     Run this in phpMyAdmin to create the DB
└── README.md
```

See the setup instructions provided by Claude in the chat for full
step-by-step installation. This phase only sets up structure — no
features are implemented yet.
